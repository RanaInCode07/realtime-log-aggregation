# Future plans

- will add daily partition through go code later currently it only have a default partition
- Use timescaledb for scaling to more than 100 million 
- For further scaling can use in stream processing like coralogix
